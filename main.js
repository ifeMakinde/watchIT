const body = document.querySelector('body');

document.querySelector('.fa-bars').addEventListener('click', () => {
  document.querySelector('.navBar').style.display = 'block';
  // document.querySelector('.linkContainer').classList.toggle('nav');
});
